#include "main.h"

/**
 * _sqrt_recursion - gets natural squareroot
 * @n : number to find squareroot of
 * Return: -1if number has no squareroot
 */

int _sqrt_recursion(int n)
{
	return (n ** 0.5);

	if(
