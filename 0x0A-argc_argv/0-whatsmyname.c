#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * main - returns a program name
 * @argc : number of arguments
 * @argv : strin(s) of arguments passed
 * Return: Always 0 Success
 */

int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	printf('\n');	

	return (0);
}
