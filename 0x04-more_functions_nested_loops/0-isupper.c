#include "main.h"

/**
 * _isupper - Checks if value is uppercase
 * @i : value to be checked
 * Return: 1 if value is upper 0 if not
 */
int _isupper(int i)
{
	int i;

	if (i >= 65 && i <= 90)
	{
	return (1);
	}
	return (0);
}
