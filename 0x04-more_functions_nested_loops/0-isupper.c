#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @c - value to be checked
 * Return : 1 if value is upper
 * Return : 0 if not
 */
int _isupper(int c)
{
	int i;

	if (i > 64 && i <= 90)
	{
	return 1;
	}
	else
	{
	return 0;
	}
}
